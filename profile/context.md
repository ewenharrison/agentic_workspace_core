# Personal Context Snapshot

## Current Snapshot
This file is the fast briefing layer for durable user-level context that may matter across projects.

## Current Objective
Build a reliable personal-context system that can sit above project memory without being confused with project-specific notes.

## Key Facts To Carry Forward
- `[P1]` `profile/` is the user-level context layer and should stay separate from `workspace/projects/`.
- `[P2]` `context.md` is the quick-start file for personal context, similar to project `memory.md`.
- `[P3]` Personal context should be used selectively and only when relevant to the current task.
- `[P4]` Store only durable, proportionate, low-risk personal context here.
- `[P5]` New or imported profile material is reviewed in `profile/working/` before it updates the durable profile files.

## Open Loops
- `[Q1]` Which user-level facts are stable enough to keep across projects?
- `[Q2]` Which preferences should be treated as durable defaults rather than one-off instructions?
- `[Q3]` How much proactive inference should be allowed into `auto/` before review?

## Next Actions
- [ ] Add approved identity and preference notes.
- [ ] Add writing-style examples or distilled style notes if useful.
- [ ] Review proposed imports in `working/` and decide what should become durable, approved, deferred, or discarded.

## Guardrails
- Treat personal context as more sensitive than project notes.
- Do not use personal context unless it helps the current task.
- Keep relationship details and sensitive facts proportionate and minimal.
- Do not confuse personal context with project memory.
